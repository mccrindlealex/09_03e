
const showExpectedResult = false
const showHints = false


const forms = document.querySelectorAll("form");

forms.forEach((form)=>{
form.addEventListener("submit", (event))
} )





function updatePage(button, argument) {
    // Your code goes here.
}